1.Simple Calculator

Write a function calculator(a, b, operator) that:

Uses operators + - * /

Uses if / else

Returns result based on operator.
calculator(10, 5, "+") // 15
2.Age Checker
let age = 20;
2.Write a condition to print:

"Child" if age < 13

"Teen" if 13–19

"Adult" if 20+

Level 2 – Intermediate
3. Grade System

Function getGrade(marks):

90+ → A

75–89 → B

50–74 → C

<50 → Fail

Use if–else.

4.Login Validation

let username = "admin";
let password = "1234";

Function login(user, pass):

If both match → "Login Success"

Else → "Invalid Credentials"

Use logical operators (&&).
5.Username Length Validator
Function validateUsername(name):

If length < 5 → "Too Short"

5–10 → "Valid"

10 → "Too Long"

6.Shopping Cart Total
let price = 200;
let quantity = 3;
let tax = 0.18;
Function getTotal(price, qty, tax):

Multiply price * qty

Add tax

Return total.
200*3 +(0.18*(200*3))

